version: "3.8"

services:
  # # postgres://directus:directus@localhost:5432/directus
  # postgres:
  #   image: postgres:17-alpine
  #   container_name: directus-postgres
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_DB: directus
  #     POSTGRES_USER: directus
  #     POSTGRES_PASSWORD: directus
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U directus -d directus"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # # MinIO S3-compatible object storage
  # minio:
  #   image: minio/minio:latest
  #   container_name: directus-minio
  #   restart: unless-stopped
  #   ports:
  #     - "9000:9000" # API
  #     - "9001:9001" # Console
  #   environment:
  #     MINIO_ROOT_USER: "minioadmin"
  #     MINIO_ROOT_PASSWORD: "minioadmin"
  #   volumes:
  #     - minio_data:/data
  #   command: server /data --console-address ":9001"
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3

  # # Create MinIO buckets on startup
  # minio-setup:
  #   image: minio/mc:latest
  #   container_name: directus-minio-setup
  #   depends_on:
  #     minio:
  #       condition: service_healthy
  #   entrypoint: >
  #     /bin/sh -c "
  #     /usr/bin/mc alias set myminio http://minio:9000 minioadmin minioadmin;
  #     /usr/bin/mc mb myminio/directus-uploads --ignore-existing;
  #     /usr/bin/mc policy set public myminio/directus-uploads;
  #     exit 0;
  #     "

  directus:
    image: "directus/directus:11"
    container_name: directus-app
    restart: unless-stopped
    ports:
      - "8055:8055"
    env_file: .env
    environment:
      PUBLIC_URL: "http://localhost:8055"

    volumes:
      - directus_uploads:/directus/uploads
      - directus_extensions:/directus/extensions
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:8055/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_data:
  directus_uploads:
  directus_extensions:
  minio_data:

networks:
  default:
    name: directus-network
